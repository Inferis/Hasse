<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Kindjes.net</title>
</head>
<body>
    <div>
        <h1>@Html.ActionLink("Kindjes.Net", "Index", "Main")</h1>

        @if (ViewBag.IsAuthenticated) {
            @ViewBag.AuthenticatedUserName @:- @Html.ActionLink("Profiel", "Index", "User") - @Html.ActionLink("Uitloggen", "SignOut", "User") 
        }
        else {
            @Html.ActionLink("Inloggen", "SignIn", "User") 
        }
    </div>
    
    <hr/>

    <div>
        @RenderBody()        
    </div>
</body>
</html>
